# Pointer file for local verification from the monorepo root.
# Real Android Gradle settings: apps/mobile/android/gradle.properties
# Release signing secrets: apps/mobile/android/keystore.properties
# (MYAPP_UPLOAD_* was replaced by storeFile, keyAlias, storePassword, keyPassword)
