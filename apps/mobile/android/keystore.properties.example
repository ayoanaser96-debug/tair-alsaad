# Copy to keystore.properties (same folder) and fill in your upload key passwords.
# Do not commit keystore.properties — it is gitignored.
#
# storeFile is relative to apps/mobile/android/app/
storeFile=tayralsaad-upload.jks
keyAlias=upload
storePassword=12081996
keyPassword=12081996
